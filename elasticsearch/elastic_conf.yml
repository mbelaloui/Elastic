# ---------------------------------- Cluster -----------------------------------
cluster.name: my-application
# ------------------------------------ Node ------------------------------------
#node.name: hostname of the node is the default name
#node.name: if we prefert to set this value in start of the instance 
# ./elasticsearch -E node.name=node_name_exemple

node.master: false
node.data: true
# ----------------------------------- Paths ------------------------------------
path.data: /var/lib/elasticsearch
path.logs: /var/log/elasticsearch
# ----------------------------------- Memory -----------------------------------
#bootstrap.memory_lock: true
# ---------------------------------- Network -----------------------------------
network.host: _site_
#http.port: 9200
# --------------------------------- Discovery ----------------------------------
cluster.initial_master_nodes: ["elastic-node-1", "elastic-node-2", "elastic-node-3"]
discovery.seed_hosts: ["elastic-node-1", "elastic-node-2", "elastic-node-3"]

path.data: /var/lib/elasticsearch
path.logs: /var/log/elasticsearch
network.host: _enp0s3_
http.port: 9200
