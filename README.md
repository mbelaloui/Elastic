# Elastic
file config

Roll

install openjdk
setup environement <swap off, Fd ulimit -n 65535, Virtual mem [sysctl -w vm.max_map_count=262144], threads [ ulimit -u 4096], >




setup ansible to setup the stack
